
git remote add origin [url of github] will create a remote to push from active repository to github.

git push origin master to push to github.

git pull origin master to update local repository from remote.